// Window.pageYoffset =  khoảng cách từ thanh scroll bar lên trên cùng
// Window.pageXOffset = khoảng cách theo chiều ngang tới phía bên trái


// scrollHeight = trả về chiều cao của element bao gồm padding, không có border
// scrollWidth : trả về chiều rộng của padding không có border